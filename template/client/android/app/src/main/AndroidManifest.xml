<?xml version="1.0" encoding="utf-8"?>
<!-- App 的清单：声明硬件要求和启动的 Activity。 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 要求设备支持 Vulkan 1.3（0x00403000 = 1.3.0）。
         不满足的设备在应用商店里看不到这个 App。 -->
    <uses-feature
        android:name="android.hardware.vulkan.version"
        android:version="0x00403000"
        android:required="true" />

    <!-- 需要网络、震动等权限时，在这里加 <uses-permission> -->

    <application
        android:label="@string/app_name"
        android:allowBackup="false"
        android:hasCode="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode|navigation">
            <!-- configChanges 列出的变化由 App 自己处理，
                 系统不会因此重建 Activity（重建会把 Vulkan 资源全丢掉）。 -->

            <!-- 这个 intent-filter 让它成为桌面图标点开的那个 Activity。 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
