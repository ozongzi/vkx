[package]
name = "vkx"
version = "0.1.4"
edition = "2024"
description = "Vulkan + SDL3 跨平台游戏工程脚手架"

[dependencies]
clap = { version = "4", features = ["derive"] }
include_dir = "0.7"

[profile.release]
strip = true
lto = true
codegen-units = 1
